<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Thomas Orta
			{% endblock %}
		</title>

		<link
		rel="stylesheet" href="{{ asset('styles/app.css') }}">

		{# Icône simple, sans SVG inline (on simplifie pour éviter la casse) #}
		<link rel="icon" href="data:,"> {% block stylesheets %}{% endblock %}

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>

	<body>
		<div class="container">
			<header class="site-header">
				<div class="brand">Thomas Orta</div>

				<nav class="nav">
					<a href="{{ path('home') }}">Accueil</a>
					<a href="{{ path('services') }}">Services</a>
					<a href="{{ path('portfolio') }}">Portfolio</a>
					<a href="{{ path('contact') }}">Contact</a>
				</nav>
			</header>

			<main>
				{% block content %}{% endblock %}
			</main>

			<footer class="site-footer">
				©
				{{ "now"|date("Y") }}
				Thomas Orta
			</footer>
		</div>
	</body>
</html>
